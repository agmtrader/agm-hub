// Array of objects or object of any
export interface Map {
    [key: string]: any
}

// Download types
export enum DeviceTypes{
  PC,
  MOBILE
}
export enum osTypes {
  WINDOWS,
  LINUX,
  MACOS,
  ANDROID,
  IOS
}
